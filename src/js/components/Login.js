import React from 'react';

require('../../scss/Login.scss');

export const Login = _ => (
	<h2>Welcome to login, <span className="darling">darling</span> or <span className="visitor">visitor</span>!</h2>
);